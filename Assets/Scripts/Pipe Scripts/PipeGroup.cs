using UnityEngine;



namespace Assets.Scripts.PipeScripts
{
    // This is only to use as a tag, this way we can find
    // in the inspector the prefab we want when selecting it
    // in the object to spawn in the PipeGroupSpawner
    [DisallowMultipleComponent]
    class PipeGroup : MonoBehaviour { }
}
